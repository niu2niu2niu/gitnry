其实这个题目本身不难，就是把链表中的数一个一个加起来。不过里面有很多细节需要考虑，比如进位的处理，链表长度不一致的处理，头结点的处理，最后的进位不要忘记等。
做的时候由于涉及到一些链表的基础操作，花了一些时间。不过整个代码基本上是自己写的。

solution2 是更简洁的代码写法。首先是把求和的式子用一行写完了，更简洁，不过可读性可能稍差。
另外链表遍历的时候指针后移的写法挺巧妙的。
还有就是把进位的处理写进主循环，也很巧妙。

solution3 更加简洁，把链表指针后移和求和放在一起，而且把进位和求和的结果放在一起，非常巧妙。
