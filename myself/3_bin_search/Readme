注意使用(left + right) / 2会有整数溢出的问题（问题会出现在当 left + right 的结果大于表达式结果类型所能表示的最大值时，这样，产生溢出后再 /2 是不会产生正确结果的，而 left + ((right - left) / 2)不存在这个问题。
注意到循环结束之后 left 和 right 的关系一定是 left > right，也就是 left = right + 1。这个对于后续的判断是非常重要的。
注意二分查找的各种变种的条件如何设置。不管条件如何变化，整体的代码框架是不变的。
