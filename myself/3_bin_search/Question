二分查找

会有很多种变体：
（1）返回第一个 =value 的元素的下标
（2）返回最后一个 =value 的元素的下标
（3）查找第一个 >value(>=value) 的元素的下标
（4）查找最后一个 <value(<=value) 的元素的下标

提问的逻辑：
1. 最简单的查找，找到就返回元素下标，没找到就返回 -1。
2. 如果有重复的，能返回第一个或最后一个 =value 的下标么？
3. 如果和 value 的关系变成 >、>=、<、<= 该怎么办呢？

题目的问法也可以考虑：如果没有找到，返回 value 应该被插入的位置。
