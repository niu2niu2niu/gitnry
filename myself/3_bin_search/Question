二分查找

会有很多种变体：
（1）返回第一次出现的元素下标
（2）返回最后一次出现的元素下标
（3）查找第一次大于（或等于）给定 value 的元素下标
（4）查找第一次小于（或等于）给定 value 的元素下标

提问的逻辑：
1.最简单的查找，找到就返回元素下标，没找到就返回 -1。
2.如果 value 在数组中重复出现，返回最左边（最右边）元素的下标，value 在数组中没有，就返回 -1。分别对应变体的（1）（2）。
3.如果 value 在数组中重复出现，返回最接近这个元素的下标。其中可以是从左边最接近，也可以是从右边最接近；最接近的定义可以是纯大于或小于，也可以包含等于。
